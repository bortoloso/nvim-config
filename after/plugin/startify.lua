vim.g.startify_session_persistence = 1
vim.g.startify_session_autoload    = 1
vim.g.startify_lists = {
    {
        type = 'sessions',
        header = {'Sessions' }},
        {
            type = 'files',
            header = {'Files'}
        }
    }

